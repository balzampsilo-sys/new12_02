# 🔒 Руководство по безопасности Telegram Booking Bot

> ⚠️ **Критически важно:** Прочтите этот гайд ПЕРЕД развертыванием в production!

---

## 🚨 Первые шаги после установки

### 1. Смена дефолтных паролей

⚠️ **КРИТИЧЕСКИ ВАЖНО!**

```bash
# Откройте .env файл
nano .env

# Измените Redis пароль
# БЫЛО:
REDIS_PASSWORD=botredis123

# СТАЛО: (используйте сложный пароль!)
REDIS_PASSWORD=Xk9#mP2$vL8qW@5nR3!zT7
```

**Генерация сложного пароля:**

```bash
# Linux/macOS
openssl rand -base64 32

# Или
head -c 32 /dev/urandom | base64

# Python (anywhere)
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

**После смены пароля:**

```bash
# Перезапустите сервисы
docker compose down
docker compose up -d --build

# Проверьте работу
docker compose logs -f bot
```

### 2. Защита Bot Token

⚠️ **НИКОГДА не публикуйте Bot Token!**

**Куда НЕ должен попасть token:**
- ❌ GitHub / GitLab / любые публичные репозитории
- ❌ Slack / Discord / мессенджеры
- ❌ Скриншоты
- ❌ Логи приложения

**Если token скомпрометирован:**

1. Немедленно откройте [@BotFather](https://t.me/BotFather)
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Нажмите: `API Token` → `Revoke current token`
5. Получите новый token
6. Обновите `.env` файл
7. Перезапустите бота

### 3. Защита файла .env

```bash
# Установите правильные права доступа
chmod 600 .env

# Проверьте
ls -la .env
# Должно быть: -rw------- (600)

# Убедитесь, что .env в .gitignore
cat .gitignore | grep .env
# Должно быть: .env
```

---

## 🔥 Настройка файервола (Firewall)

### Ubuntu/Debian (UFW)

```bash
# Установка UFW (если нет)
sudo apt update
sudo apt install ufw

# Разрешить SSH (ЧТОБЫ НЕ ПОТЕРЯТЬ ДОСТУП!)
sudo ufw allow 22/tcp

# Запретить все остальное по умолчанию
sudo ufw default deny incoming
sudo ufw default allow outgoing

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status verbose
```

**Важно:**
- ❌ **НЕ открывайте** порт 6379 (Redis) во внешний интернет!
- ❌ **НЕ открывайте** порты базы данных
- ✅ Redis и SQLite доступны только внутри Docker сети

### CentOS/RHEL (firewalld)

```bash
# Установка
sudo yum install firewalld
sudo systemctl start firewalld
sudo systemctl enable firewalld

# Разрешить SSH
sudo firewall-cmd --permanent --add-service=ssh

# Применить
sudo firewall-cmd --reload

# Проверить
sudo firewall-cmd --list-all
```

---

## 🔐 Защита SSH

### 1. Отключить вход по паролю

```bash
# Редактировать sshd_config
sudo nano /etc/ssh/sshd_config

# Найдите и измените:
PasswordAuthentication no
PermitRootLogin no
PubkeyAuthentication yes

# Перезапустить SSH
sudo systemctl restart sshd
```

⚠️ **Перед отключением:**
- Убедитесь, что SSH-ключ установлен и работает!
- Проверьте вход в новом терминале ПЕРЕД закрытием текущей сессии!

### 2. Изменить порт SSH (опционально)

```bash
# В /etc/ssh/sshd_config
Port 2222  # вместо 22

# Добавить в firewall
sudo ufw allow 2222/tcp
sudo ufw delete allow 22/tcp

# Перезапустить
sudo systemctl restart sshd
```

### 3. Fail2Ban - защита от brute-force

```bash
# Установка
sudo apt install fail2ban

# Создать конфигурацию
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local

# Редактировать
sudo nano /etc/fail2ban/jail.local

# Настройки:
[sshd]
enabled = true
port = 22  # или ваш порт
maxretry = 3
bantime = 3600  # 1 час
findtime = 600

# Запустить
sudo systemctl enable fail2ban
sudo systemctl start fail2ban

# Проверить статус
sudo fail2ban-client status sshd
```

---

## 💾 Безопасность бэкапов

### 1. Автоматические бэкапы

Бот уже создает автобэкапы в `./backups/`, но нужно:

**Защитить директорию:**

```bash
# Ограничить доступ
chmod 700 backups/
chmod 600 backups/*.db

# Проверить
ls -la backups/
```

### 2. Оффсайт бэкапы

⚠️ **КРИТИЧЕСКИ ВАЖНО: храните бэкапы ВНЕ сервера!**

**Вариант 1: rsync на удаленный сервер**

```bash
# Создать скрипт backup-offsite.sh
nano ~/backup-offsite.sh
```

```bash
#!/bin/bash

BACKUP_DIR="/path/to/tg-bot-10_02/backups"
REMOTE_USER="backup-user"
REMOTE_HOST="backup.example.com"
REMOTE_DIR="/backups/telegram-bot"

# Копирование с шифрованием
rsync -avz --delete \
    -e "ssh -p 22" \
    "$BACKUP_DIR/" \
    "${REMOTE_USER}@${REMOTE_HOST}:${REMOTE_DIR}/"

echo "[$(date)] Backup completed" >> ~/backup.log
```

```bash
# Сделать исполняемым
chmod +x ~/backup-offsite.sh

# Добавить в crontab (каждый день в 3:30)
crontab -e
# Добавьте:
30 3 * * * /home/your-user/backup-offsite.sh
```

**Вариант 2: Загрузка в облако (AWS S3)**

```bash
# Установить AWS CLI
sudo apt install awscli

# Настроить
aws configure

# Скрипт загрузки
nano ~/backup-s3.sh
```

```bash
#!/bin/bash

BACKUP_DIR="/path/to/tg-bot-10_02/backups"
S3_BUCKET="s3://your-bucket-name/telegram-bot-backups"

# Загрузка с шифрованием
aws s3 sync "$BACKUP_DIR/" "$S3_BUCKET/" \
    --storage-class STANDARD_IA \
    --sse AES256

echo "[$(date)] S3 backup completed" >> ~/backup-s3.log
```

### 3. Шифрование бэкапов

```bash
# Установить gpg
sudo apt install gnupg

# Создать ключ
gpg --gen-key

# Шифрование бэкапа
gpg --encrypt --recipient your-email@example.com backup.db

# Расшифровка (при восстановлении)
gpg --decrypt backup.db.gpg > backup.db
```

---

## 🔍 Мониторинг безопасности

### 1. Проверка логов SSH

```bash
# Последние попытки входа
sudo grep "Failed password" /var/log/auth.log | tail -20

# Успешные входы
sudo grep "Accepted" /var/log/auth.log | tail -20

# Текущие SSH сессии
who
w
```

### 2. Мониторинг активности бота

```bash
# Проверка ошибок
docker compose logs bot | grep ERROR | tail -50

# Подозрительная активность
docker compose logs bot | grep "rate_limit" | tail -20

# Статистика Redis
docker compose exec redis redis-cli -a $REDIS_PASSWORD info stats
```

### 3. Мониторинг ресурсов

```bash
# Использование диска
df -h
du -sh /path/to/tg-bot-10_02/*

# Использование Docker
docker stats --no-stream

# Размер логов
du -sh /var/lib/docker/containers/*
```

---

## 🔄 Обновления и патчи

### 1. Обновление системы

```bash
# Ubuntu/Debian
sudo apt update
sudo apt upgrade -y
sudo apt autoremove -y

# CentOS/RHEL
sudo yum update -y
sudo yum autoremove -y

# Перезагрузка (если нужно)
sudo reboot
```

### 2. Обновление Docker

```bash
# Проверить текущую версию
docker --version

# Обновить
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io

# Перезапустить Docker
sudo systemctl restart docker
```

### 3. Обновление бота

```bash
# Создать бэкап ПЕРЕД обновлением
cp data/bookings.db backups/pre-update-$(date +%Y%m%d-%H%M%S).db

# Обновить код
git pull origin main

# Пересобрать и перезапустить
docker compose down
docker compose up -d --build

# Проверить логи
docker compose logs -f bot
```

---

## 🚨 Реагирование на инциденты

### Подозрение на взлом

**Немедленные действия:**

1. **Остановить бота**
```bash
docker compose down
```

2. **Сменить все пароли**
- Bot Token (через @BotFather)
- Redis пароль
- SSH пароли/ключи

3. **Проверить логи**
```bash
sudo grep "Failed\|Accepted" /var/log/auth.log | tail -100
docker compose logs bot | grep -i "error\|warning" | tail -100
```

4. **Проверить файлы**
```bash
find /path/to/tg-bot-10_02 -type f -mtime -1  # файлы измененные за 24ч
```

5. **Проверить сетевые соединения**
```bash
sudo netstat -tulpn | grep ESTABLISHED
sudo ss -tulpn
```

6. **Восстановить из чистого бэкапа**
```bash
# Если данные скомпрометированы
rm data/bookings.db
cp backups/safe-backup.db data/bookings.db
```

7. **Запустить с новыми паролями**
```bash
docker compose up -d --build
```

---

## 📝 Security Checklist

Используйте этот чеклист при развертывании:

### Первоначальная настройка

- [ ] Сменен Redis пароль на сложный
- [ ] Bot Token хранится только в .env
- [ ] .env имеет права 600
- [ ] .env добавлен в .gitignore
- [ ] Установлен firewall (ufw/firewalld)
- [ ] SSH использует ключи (не пароли)
- [ ] Установлен Fail2Ban

### Бэкапы

- [ ] Автобэкапы работают
- [ ] Настроен offsite бэкап
- [ ] Бэкапы защищены (chmod 600)
- [ ] Проверена возможность восстановления

### Мониторинг

- [ ] Настроен мониторинг логов
- [ ] Настроены алерты на ошибки
- [ ] Проверяется использование диска

### Обновления

- [ ] Обновления системы регулярны
- [ ] Docker обновлен до последней версии
- [ ] Бот обновляется из репозитория

### Docker Security

- [ ] Порт Redis (6379) НЕ открыт во внешний интернет
- [ ] Docker daemon защищен
- [ ] Контейнеры запускаются с ограниченными правами

---

## 📚 Дополнительные ресурсы

### Лучшие практики

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Docker Security Best Practices](https://docs.docker.com/engine/security/)
- [Telegram Bot API Security](https://core.telegram.org/bots/faq#how-do-i-make-sure-that-my-bot-is-secure)
- [SSH Hardening Guide](https://www.ssh.com/academy/ssh/security)

### Инструменты аудита

```bash
# Сканирование портов
sudo nmap -sV localhost

# Проверка уязвимостей Docker
docker scan <image-name>

# Проверка конфигурации SSH
ssh-audit <your-server-ip>
```

---

## 📧 Контакты для сообщений о багах безопасности

Если вы обнаружили уязвимость в боте:

1. **Не публикуйте** детали публично
2. Создайте **private issue** на GitHub
3. Или отправьте email разработчику

---

## ⚠️ Важные напоминания

✅ **ДЕЛАЙТЕ:**
- Регулярно обновляйте систему
- Мониторьте логи
- Делайте бэкапы ВНЕ сервера
- Используйте сложные пароли
- Проверяйте бэкапы периодически

❌ **НЕ ДЕЛАЙТЕ:**
- Публиковать Bot Token
- Использовать дефолтные пароли
- Открывать порты Redis/SQLite вовне
- Запускать контейнеры с root
- Игнорировать обновления безопасности

---

**Версия:** 1.0.0  
**Последнее обновление:** 12 февраля 2026  
**Статус:** 🔒 Критически важно

🔒 **Безопасность - это не однократное действие, а постоянный процесс!**
