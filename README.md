# 🤖 Booking Bot - Multi-Tenant SaaS Platform

**Telegram бот для бронирования услуг с неограниченным количеством клиентов**

[![Docker](https://img.shields.io/badge/Docker-Ready-blue?logo=docker)](https://www.docker.com/)
[![Python](https://img.shields.io/badge/Python-3.11-green?logo=python)](https://www.python.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue?logo=postgresql)](https://www.postgresql.org/)
[![Redis](https://img.shields.io/badge/Redis-7-red?logo=redis)](https://redis.io/)
[![aiogram](https://img.shields.io/badge/aiogram-3.21-blue)](https://github.com/aiogram/aiogram)

---

## 📋 О проекте

Профессиональный Telegram-бот для автоматизации записи клиентов на услуги:
- ✂️ Салоны красоты
- 💆 Массажные кабинеты  
- 💅 Ногтевые студии
- 💈 Барбершопы
- 🏥 Медицинские клиники
- 📚 Консультации и обучение

**Multi-Tenant SaaS:** Один сервер → Неограниченное количество ботов → Полная изоляция данных через PostgreSQL schemas.

---

## ✨ Ключевые возможности

### 🎯 **Бизнес-функции**

#### 👤 Для клиентов:
- 📅 Интерактивный календарь бронирования
- ⏰ Автоматические напоминания (24ч/2ч/1ч до визита)
- 🔄 Отмена и перенос записей (с контролем сроков)
- ⭐ Система оценок и отзывов
- 📱 Адаптивный интерфейс

#### 👨‍💼 Для администратора:
- 📊 Dashboard с real-time аналитикой
- 🗓️ Расписание на неделю/месяц
- ⚡ Массовые операции (перенос времени, смена услуг)
- 🔧 Управление услугами, ценами, длительностью
- 🚫 Блокировка слотов с уведомлениями
- 👥 Управление администраторами (role-based)
- 📝 Редактор текстов интерфейса (i18n)
- 📈 Экспорт данных и статистика
- 📜 Полный audit log всех действий

### 🚀 **Технические преимущества**

- ✅ **Неограниченное масштабирование** (PostgreSQL schemas + Redis key prefixes)
- ✅ **Полная изоляция данных** между клиентами
- ✅ **Автоматическое создание** schemas при запуске
- ✅ **Connection pooling** для производительности
- ✅ **Автоматические бэкапы** (pg_dump)
- ✅ **Rate limiting** и защита от флуда
- ✅ **Sentry мониторинг** ошибок
- ✅ **Health checks** и auto-restart
- ✅ **Production-ready** конфигурация

---

## 🏗️ Архитектура

### **Multi-Tenant Isolation через PostgreSQL Schemas**

```
┌────────────────────────────────────────────────────────┐
│     PostgreSQL (booking_saas) - ЕДИНАЯ БД              │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Schema: master_bot                               │ │
│  │  • 12 таблиц (bookings, services, users...)     │ │
│  │  • 16 индексов для производительности            │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Schema: sales_bot                                │ │
│  │  • 12 таблиц (bookings, services, users...)     │ │
│  │  • 16 индексов                                   │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ Schema: client_003 (и так далее...)              │ │
│  │  • Автоматически создается при запуске           │ │
│  └──────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────┘
              ▲              ▲              ▲
              │              │              │
        ┌─────────┐    ┌─────────┐    ┌─────────┐
        │Master   │    │Sales    │    │Client   │
        │Bot      │    │Bot      │    │003      │
        └─────────┘    └─────────┘    └─────────┘
              │              │              │
              ▼              ▼              ▼
┌────────────────────────────────────────────────────────┐
│     Redis - FSM States (Key Prefixes)                  │
│                                                        │
│  • master_bot:user:123:state                          │
│  • sales_bot:user:456:state                           │
│  • client_003:user:789:state                          │
└────────────────────────────────────────────────────────┘
```

**Преимущества:**
- 🚀 **Неограниченное кол-во клиентов** (schemas, не отдельные БД)
- 🔒 **SQL-уровень изоляции** (search_path автоматически)
- ⚡ **Единый connection pool** (эффективное использование ресурсов)
- 💾 **Централизованные бэкапы** (один pg_dump для всех)

---

## 🚀 Быстрый старт (5 минут)

### Вариант 1: Docker Compose (рекомендуется)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/balzampsilo-sys/new12_02.git
cd new12_02

# 2. Настроить .env
cp .env.example .env
nano .env

# Указать токены ОБОИХ ботов:
BOT_TOKEN_MASTER=your_master_bot_token
BOT_TOKEN_SALES=your_sales_bot_token
ADMIN_IDS_MASTER=your_telegram_id
ADMIN_IDS_SALES=your_telegram_id

# 3. Запустить ВСЁ одной командой
docker-compose up -d

# 4. Проверить статус
docker-compose ps

# Должны увидеть:
# ✅ booking-postgres     Up (healthy)
# ✅ booking-redis        Up (healthy)
# ✅ booking-bot-master   Up
# ✅ booking-bot-sales    Up

# 5. Просмотреть логи
docker-compose logs -f bot-master

# Должны увидеть:
# 📦 Initializing schema: master_bot
#   ✅ Schema created: master_bot
#   ✅ Created 12 tables
#   ✅ Created 16 indexes
# 🤖 Bot started successfully
```

**Готово!** Оба бота запущены и работают! 🎉

---

### Вариант 2: Локальная разработка

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Запустить PostgreSQL + Redis
docker-compose -f docker-compose.postgres.yml up -d

# 3. Настроить .env для ОДНОГО бота
cp .env.example .env
nano .env

BOT_TOKEN=your_token
ADMIN_IDS=your_id
CLIENT_ID=dev_bot
DB_TYPE=postgresql
DATABASE_URL=postgresql://booking_user:SecurePass2026!@localhost:5432/booking_saas

# 4. Запустить бота
python main.py
```

---

## 📖 Полная документация

📚 **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Подробное руководство по развертыванию

Включает:
- ✅ Проверка изоляции данных
- ✅ Добавление новых ботов
- ✅ Мониторинг и логи
- ✅ PostgreSQL бэкапы
- ✅ Troubleshooting
- ✅ Безопасность

📊 **[FIX_PROGRESS.md](./FIX_PROGRESS.md)** - Статус миграции на PostgreSQL (85% готово)

---

## 🛠️ Технологический стек

### Backend
- **Python 3.11** - Современная версия с улучшенной производительностью
- **aiogram 3.21** - Асинхронный фреймворк для Telegram Bot API
- **PostgreSQL 16** - Production-ready СУБД с multi-tenant поддержкой
- **asyncpg** - Быстрый PostgreSQL драйвер
- **Redis 7** - FSM states с key prefix изоляцией

### Инфраструктура
- **Docker & Docker Compose** - Контейнеризация
- **APScheduler** - Cron jobs (напоминания, бэкапы)
- **Sentry** - Мониторинг ошибок в production

### Основные зависимости
```txt
aiogram==3.21.0
asyncpg==0.30.0
redis==5.2.1
APScheduler==3.10.4
sentry-sdk==2.19.2
python-dotenv==1.0.1
PyYAML==6.0.2
```

---

## ⚡ Добавление нового бота (2 минуты)

### Шаг 1: Добавить в docker-compose.yml

```yaml
  bot-newclient:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: booking-bot-newclient
    environment:
      BOT_TOKEN: ${BOT_TOKEN_NEWCLIENT}
      ADMIN_IDS: ${ADMIN_IDS_NEWCLIENT}
      CLIENT_ID: newclient
      DB_TYPE: postgresql
      DATABASE_URL: postgresql://booking_user:${POSTGRES_PASSWORD}@postgres:5432/booking_saas
      PG_SCHEMA: newclient  # ✅ Уникальная schema
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_KEY_PREFIX: "newclient:"  # ✅ Уникальный prefix
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - booking-network
```

### Шаг 2: Добавить в .env

```env
BOT_TOKEN_NEWCLIENT=your_new_bot_token
ADMIN_IDS_NEWCLIENT=your_admin_id
```

### Шаг 3: Запустить

```bash
docker-compose up -d bot-newclient

# Schema "newclient" создастся автоматически!
# ✅ 12 таблиц
# ✅ 16 индексов
# ✅ Готово к работе
```

**Вот и всё!** Новый бот работает с полной изоляцией данных. 🚀

---

## 📊 Управление ботами

### Просмотр статуса всех ботов
```bash
docker-compose ps
```

### Перезапуск конкретного бота
```bash
docker-compose restart bot-master
```

### Остановка бота (например, неоплата)
```bash
docker-compose stop bot-sales
```

### Возобновление бота
```bash
docker-compose start bot-sales
```

### Просмотр логов
```bash
# Все боты
docker-compose logs -f

# Конкретный бот
docker-compose logs -f bot-master

# Последние 100 строк
docker-compose logs --tail=100 bot-master
```

### Обновление кода
```bash
git pull
docker-compose build
docker-compose up -d
```

---

## 🔧 Конфигурация

### Основные настройки (.env)

```env
# ========================================
# BOT CONFIGURATION
# ========================================
BOT_TOKEN=your_bot_token
ADMIN_IDS=123456789,987654321
CLIENT_ID=master_bot  # Уникальный ID

# ========================================
# DATABASE (PostgreSQL)
# ========================================
DB_TYPE=postgresql
DATABASE_URL=postgresql://user:pass@host:5432/booking_saas
PG_SCHEMA=master_bot  # Уникальная schema

# ========================================
# REDIS (FSM States)
# ========================================
REDIS_ENABLED=true
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_KEY_PREFIX=master_bot:  # Уникальный prefix

# ========================================
# BUSINESS LOGIC
# ========================================
WORK_HOURS_START=9
WORK_HOURS_END=21
SLOT_INTERVAL_MINUTES=60
MAX_BOOKINGS_PER_USER=3
CANCELLATION_HOURS=24

# ========================================
# MONITORING (Optional)
# ========================================
SENTRY_ENABLED=false
SENTRY_DSN=your_sentry_dsn
```

---

## 📈 Требования к серверу

### Рекомендуемые конфигурации

| Ботов | CPU | RAM | Диск | Примечание |
|-------|-----|-----|------|------------|
| 1-5   | 2   | 2GB | 20GB | Стартовая конфигурация |
| 5-20  | 2   | 4GB | 40GB | Малый бизнес |
| 20-50 | 4   | 8GB | 100GB | Средний бизнес |
| 50+   | 8   | 16GB | 200GB | Масштаб (+ load balancer) |

### Системные требования
- **ОС:** Ubuntu 22.04 LTS (рекомендуется)
- **Docker:** 24.0+
- **Docker Compose:** 2.20+
- **Диск:** SSD обязательно
- **Сеть:** Минимум 100 Мбит/с

### PostgreSQL тюнинг

В `docker-compose.yml` уже настроены оптимальные параметры:
```yaml
shared_buffers=256MB
max_connections=100
effective_cache_size=1GB
work_mem=4MB
```

---

## ✅ Production Checklist

Перед запуском в production:

- [ ] Изменить пароли PostgreSQL и Redis
- [ ] Настроить SSL сертификаты (Let's Encrypt)
- [ ] Настроить firewall (закрыть порты 5432, 6379)
- [ ] Настроить автоматические бэкапы (cron)
- [ ] Подключить Sentry для мониторинга
- [ ] Настроить логирование (ELK/Grafana)
- [ ] Ограничить SSH доступ
- [ ] Настроить мониторинг ресурсов
- [ ] Документировать процедуры восстановления

---

## 🔒 Безопасность

### Изоляция данных
- ✅ **SQL-уровень:** PostgreSQL schemas с `search_path`
- ✅ **Redis-уровень:** Уникальные key prefixes
- ✅ **Код-уровень:** CLIENT_ID валидация

### Защита от атак
- ✅ **Rate limiting:** Защита от флуда
- ✅ **SQL injection:** Параметризованные запросы
- ✅ **Input validation:** Проверка всех входных данных
- ✅ **Non-root containers:** Docker security

---

## 🐛 Troubleshooting

### Бот не запускается

```bash
# Проверить логи
docker-compose logs bot-master

# Типичные причины:
# 1. Неверный BOT_TOKEN
# 2. PostgreSQL недоступен
# 3. Redis недоступен
```

### Schema не создается

```bash
# Проверить права пользователя
docker-compose exec postgres psql -U booking_user -d booking_saas

# Должны быть права:
GRANT ALL ON SCHEMA public TO booking_user;
```

### Бот не отвечает

```bash
# Проверить health checks
docker-compose ps

# Перезапустить
docker-compose restart bot-master
```

---

## 📦 Бэкапы

### Автоматический бэкап всех schemas

```bash
# Добавить в cron (ежедневно в 2:00)
0 2 * * * docker-compose exec postgres pg_dump -U booking_user booking_saas > /backups/booking_$(date +\%Y\%m\%d).sql
```

### Бэкап конкретной schema

```bash
docker-compose exec postgres pg_dump -U booking_user -n master_bot booking_saas > master_bot_backup.sql
```

### Восстановление

```bash
docker-compose exec -T postgres psql -U booking_user booking_saas < backup.sql
```

---

## 🎯 Roadmap

### В разработке
- [ ] Web админ-панель (FastAPI + React)
- [ ] GraphQL API для интеграций
- [ ] Мобильное приложение (Flutter)
- [ ] AI-ассистент для автоответов
- [ ] Интеграция с календарями (Google/Outlook)

### Планируется
- [ ] Платежные системы (Stripe/YooKassa)
- [ ] SMS-уведомления (Twilio)
- [ ] Система лояльности
- [ ] Kubernetes deployment
- [ ] Horizontal scaling

---

## 🤝 Поддержка

- 🐛 **Баги:** [GitHub Issues](https://github.com/balzampsilo-sys/new12_02/issues)
- 📖 **Документация:** [DEPLOYMENT.md](./DEPLOYMENT.md)
- 💬 **Вопросы:** Telegram [@your_support_bot]

---

## 📝 Лицензия

Проприетарная. Код предназначен для managed SaaS модели.

---

## 🌟 Особенности проекта

✅ **Production-Ready** - Готов к запуску в production  
✅ **Unlimited Scaling** - Неограниченное количество ботов  
✅ **Full Isolation** - Полная изоляция данных  
✅ **Auto-Setup** - Автоматическое создание schemas  
✅ **High Performance** - Connection pooling + indexes  
✅ **Zero Downtime** - Health checks + auto-restart  
✅ **Easy Deployment** - Одна команда для запуска  
✅ **Multi-Tenant** - PostgreSQL schemas архитектура  

---

**Разработано для SaaS:** Один сервер → Неограниченное количество ботов → Полная изоляция

🚀 **Готов к деплою прямо сейчас!**
