version: '3.8'

# ✅ Multi-Bot Deployment: Master Bot + Sales Bot
# Каждый бот использует свою schema в PostgreSQL
# и свой key prefix в Redis

services:
  # PostgreSQL - общая БД для всех клиентов
  postgres:
    image: postgres:16-alpine
    container_name: booking-postgres
    environment:
      POSTGRES_USER: booking_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-SecurePass2026!}
      POSTGRES_DB: booking_saas
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=en_US.UTF-8"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U booking_user -d booking_saas"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - booking-network
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "max_connections=100"
      - "-c"
      - "log_min_duration_statement=1000"

  # Redis - общий Redis с key prefixes для изоляции
  redis:
    image: redis:7-alpine
    container_name: booking-redis
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - booking-network

  # ✅ MASTER BOT - управление подписками и клиентами
  bot-master:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: booking-bot-master
    command: python master_bot/master_bot.py  # ✅ FIXED: запускаем правильный файл
    environment:
      # Bot Configuration
      BOT_TOKEN: ${BOT_TOKEN_MASTER}
      ADMIN_IDS: ${ADMIN_IDS_MASTER}
      
      # ✅ Multi-tenant: уникальный CLIENT_ID
      CLIENT_ID: master_bot
      
      # PostgreSQL
      DB_TYPE: postgresql
      DATABASE_URL: postgresql://booking_user:${POSTGRES_PASSWORD:-SecurePass2026!}@postgres:5432/booking_saas
      PG_SCHEMA: master_bot  # ✅ Отдельная schema
      
      # Redis
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_DB: 0
      REDIS_KEY_PREFIX: "master_bot:"  # ✅ Уникальный prefix
      
      # Business Logic
      TIMEZONE: ${TIMEZONE:-Europe/Moscow}
      WORK_HOURS_START: ${WORK_HOURS_START:-9}
      WORK_HOURS_END: ${WORK_HOURS_END:-21}
      SLOT_INTERVAL_MINUTES: ${SLOT_INTERVAL_MINUTES:-60}
      MAX_BOOKINGS_PER_USER: ${MAX_BOOKINGS_PER_USER:-3}
      CANCELLATION_HOURS: ${CANCELLATION_HOURS:-24}
      
      # Sentry (optional)
      SENTRY_ENABLED: ${SENTRY_ENABLED:-false}
      SENTRY_DSN: ${SENTRY_DSN_MASTER:-}
      SENTRY_ENVIRONMENT: production-master
      
      # Backup (disabled for PostgreSQL)
      BACKUP_ENABLED: "false"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - booking-network
    volumes:
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ✅ SALES BOT - продажа подписок с ЮKassa
  bot-sales:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: booking-bot-sales
    command: python sales_bot/sales_bot.py  # ✅ FIXED: запускаем правильный файл
    environment:
      # Bot Configuration
      BOT_TOKEN: ${BOT_TOKEN_SALES}
      ADMIN_IDS: ${ADMIN_IDS_SALES}  # ✅ ID администраторов салес бота
      
      # ✅ Multi-tenant: уникальный CLIENT_ID
      CLIENT_ID: sales_bot
      
      # PostgreSQL
      DB_TYPE: postgresql
      DATABASE_URL: postgresql://booking_user:${POSTGRES_PASSWORD:-SecurePass2026!}@postgres:5432/booking_saas
      PG_SCHEMA: sales_bot  # ✅ Отдельная schema
      
      # Redis
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_DB: 0
      REDIS_KEY_PREFIX: "sales_bot:"  # ✅ Уникальный prefix
      
      # Business Logic (можно указать другие значения)
      TIMEZONE: ${TIMEZONE:-Europe/Moscow}
      WORK_HOURS_START: ${WORK_HOURS_START_SALES:-9}
      WORK_HOURS_END: ${WORK_HOURS_END_SALES:-21}
      SLOT_INTERVAL_MINUTES: ${SLOT_INTERVAL_MINUTES_SALES:-60}
      MAX_BOOKINGS_PER_USER: ${MAX_BOOKINGS_PER_USER_SALES:-3}
      CANCELLATION_HOURS: ${CANCELLATION_HOURS_SALES:-24}
      
      # Sentry (optional)
      SENTRY_ENABLED: ${SENTRY_ENABLED:-false}
      SENTRY_DSN: ${SENTRY_DSN_SALES:-}
      SENTRY_ENVIRONMENT: production-sales
      
      # Backup (disabled for PostgreSQL)
      BACKUP_ENABLED: "false"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - booking-network
    volumes:
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  booking-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
