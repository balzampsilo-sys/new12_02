# 🎯 Интеграция управления услугами в админ-панель

## ✅ Что добавлено

1. **utils/states.py** - Добавлены FSM состояния для управления услугами
2. **handlers/service_admin_handlers.py** - Обработчики для добавления услуг
3. **handlers/service_admin_handlers_part2.py** - Обработчики для редактирования/удаления
4. **database/repositories/service_repository_extended.py** - Дополнительные методы

---

## 🚀 Шаг 1: Обновить keyboards/admin_keyboards.py

Добавьте кнопку "🎯 Услуги" в ADMIN_MENU:

```python
from aiogram.types import KeyboardButton, ReplyKeyboardMarkup

ADMIN_MENU = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(text="📊 Dashboard"), KeyboardButton(text="📅 Расписание")],
        [KeyboardButton(text="👥 Клиенты"), KeyboardButton(text="💡 Рекомендации")],
        [KeyboardButton(text="🎯 Услуги"), KeyboardButton(text="📊 Экспорт данных")],  # НОВАЯ СТРОКА
        [KeyboardButton(text="⚡ Массовые операции")],
        [KeyboardButton(text="🔙 Выход из админки")],
    ],
    resize_keyboard=True,
)
```

---

## 🚀 Шаг 2: Добавить обработчик кнопки в handlers/admin_handlers.py

Добавьте перед `@router.message(F.text == "⚡ Массовые операции")`:

```python
@router.message(F.text == "🎯 Услуги")
async def services_button_handler(message: Message):
    """Обработка кнопки Услуги"""
    if not is_admin(message.from_user.id):
        await message.answer("❌ Нет доступа")
        return
    
    # Создаем callback для перехода в меню услуг
    from aiogram.types import CallbackQuery, InlineKeyboardButton, InlineKeyboardMarkup
    
    kb = InlineKeyboardMarkup(inline_keyboard=[[
        InlineKeyboardButton(text="🔎 Открыть меню", callback_data="admin_services")
    ]])
    
    await message.answer(
        "🎯 УПРАВЛЕНИЕ УСЛУГАМИ\n\n"
        "Здесь вы можете:\n"
        "• Добавлять новые услуги\n"
        "• Редактировать существующие\n"
        "• Включать/отключать услуги\n"
        "• Изменять порядок отображения",
        reply_markup=kb
    )
```

---

## 🚀 Шаг 3: Регистрация роутеров в main.py

Добавьте импорты:

```python
# Добавьте после существующих импортов handlers
from handlers import service_admin_handlers, service_admin_handlers_part2
```

Зарегистрируйте роутеры (добавьте перед `dp.include_router(admin_handlers.router)`):

```python
# Регистрация роутеров (порядок важен!)
dp.include_router(service_admin_handlers.router)        # НОВОЕ
dp.include_router(service_admin_handlers_part2.router)  # НОВОЕ
dp.include_router(user_handlers.router)
dp.include_router(booking_handlers.router)
dp.include_router(admin_handlers.router)
```

---

## 🚀 Шаг 4: Обновить database/repositories/__init__.py

Добавьте импорт расширенных методов:

```python
from database.repositories.analytics_repository import (
    AnalyticsRepository,
    ClientStats,
)
from database.repositories.booking_repository import BookingRepository
from database.repositories.user_repository import UserRepository
from database.repositories.service_repository_extended import ServiceRepositoryExtended  # НОВОЕ

# Алиас для удобства
ServiceRepository = ServiceRepositoryExtended  # НОВОЕ

__all__ = [
    "BookingRepository",
    "UserRepository",
    "AnalyticsRepository",
    "ClientStats",
    "ServiceRepository",  # НОВОЕ
]
```

---

## ✅ Проверка работы

### 1. Запустите бота:

```bash
python main.py
```

### 2. Откройте админ-панель:

```
/admin
```

### 3. Нажмите кнопку "🎯 Услуги"

Вы должны увидеть меню с кнопками:
- ➕ Добавить услугу
- ✏️ Редактировать
- 🔄 Изменить порядок

---

## 🎉 Функционал

### ➕ Добавление услуги

Пошаговый мастер:
1. Название (например: "VIP консультация")
2. Описание (можно пропустить с "-")
3. Длительность в минутах (15-480)
4. Цена (например: "5000 ₽")

✅ Услуга сразу становится доступной для записи!

### ✏️ Редактирование

Выберите услугу из списка, затем:
- Изменить название
- Изменить описание
- Изменить длительность
- Изменить цену
- ✅/🚫 Включить/Отключить
- 🗑️ Удалить

### 🔄 Изменение порядка

Перемещайте услуги вверх ⬆️ или вниз ⬇️ 
Порядок сразу отображается пользователям при выборе услуги.

---

## ⚠️ ВАЖНО

- Отключенные услуги (🚫) не показываются пользователям
- При удалении услуги проверяется наличие записей
- Изменения применяются мгновенно
- Все действия логируются

---

## 📝 Пример использования

### Добавление новой услуги:

1. 🎯 Услуги → ➕ Добавить услугу
2. Вводите: "VIP консультация"
3. Вводите: "Максимально детальная консультация 120 минут"
4. Вводите: 120
5. Вводите: "5500 ₽"
6. ✅ Готово!

### Изменение цены:

1. 🎯 Услуги → ✏️ Редактировать
2. Выберите услугу
3. 💰 Изменить цену
4. Введите новую цену
5. ✅ Готово!

---

## ✅ Готово!

Теперь вы можете управлять услугами прямо из Telegram! 🎉
