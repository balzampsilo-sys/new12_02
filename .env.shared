# ============================================
# SHARED CONFIGURATION FOR ALL CLIENTS
# ============================================
# This file contains credentials used by ALL bots
# Copy this file, never commit with real passwords!

# PostgreSQL Configuration
POSTGRES_HOST=postgres-shared
POSTGRES_PORT=5432
POSTGRES_ADMIN_USER=booking_admin
POSTGRES_ADMIN_PASSWORD=changeme_admin_password
POSTGRES_DEFAULT_DB=postgres

# Redis Configuration
REDIS_HOST=redis-shared
REDIS_PORT=6379
REDIS_ENABLED=true

# Shared Database Credentials
# All clients use these credentials (with separate databases)
DB_USER_PASSWORD=secure_client_password_2026
DB_TYPE=postgresql

# Connection Pool Settings
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_POOL_TIMEOUT=30.0
DB_COMMAND_TIMEOUT=60.0

# Network Configuration
NETWORK_NAME=new12_02_booking-network
